<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Audio I/O -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Background service (FOREGROUND_SERVICE requires API 28; ignored on older devices) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <!-- FOREGROUND_SERVICE_MICROPHONE requires API 34; safe to declare on older devices -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
        tools:ignore="ProtectedPermissions" />

    <!-- Keep CPU awake during processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceFlux"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service so processing survives background.
             foregroundServiceType="microphone" is ignored on API < 29 and required on API 29+. -->
        <service
            android:name=".AudioService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            tools:targetApi="29" />

    </application>

</manifest>
